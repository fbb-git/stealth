#include "lock.ih"

void Lock::unlockRunFile()
{
//    if (s_runFILE)
//    {
//        flock(fileno(s_runFILE), LOCK_UN);        
//        fclose(s_runFILE);              // closing removes the lock
//    }
//    s_runFILE = 0;
}
