#include "stealth.ih"

void Stealth::processMail()
{
    if (d_options.logMail())
    {
        *d_report.rewind();

        string line;
        while (getline(*d_report.in(), line))
            imsg << "Mail: " << line << endl;
    }
    
    if (d_options.sendMail())
        sendMail();
}





