req()
{
    echo "$*"
    $* || exit 1
}

reqeval()
{
    echo "$*"
    eval "$*" || exit 1
}

reqzip()
{
    DEST=$1$DOC
    shift

    for x in $*
    do
        reqeval "gzip < $x > $DEST/`basename $x`.gz"
    done
}

    