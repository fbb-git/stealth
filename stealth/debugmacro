#ifndef _DEBUGMACRO_
#define _DEBUGMACRO_

#ifdef DEBUG
    #include <iostream>
    #include "util/util.h"
    
    #define dout(x) if(Util::debug())std::cerr<<x<<std::endl
    #define dot()   if(Util::debug())std::cerr<<"."
#else
    #define dout(x)
    #define dot(x)
#endif

#endif
