#ifndef _DEBUGMACRO_
#define _DEBUGMACRO_

#ifdef DEBUG

#include <iostream>
#include <unistd.h>
#include "arg/arg.h"

#define dout(x) if(FBB::Arg::getInstance().option('d'))std::cerr<<x<<std::endl
#else
#define dout(x)
#endif

#endif

